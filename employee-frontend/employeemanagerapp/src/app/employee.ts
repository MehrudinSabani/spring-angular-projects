export interface Employee{
    //we just miror the employee from the backend
    id: number;
    name: String;
    email: String;
    jobTitle: String;
    phone: String;
    imageUrl: String;
    employeeCode: string;
}